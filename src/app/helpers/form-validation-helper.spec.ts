import { FormValidationHelper } from './form-validation-helper';

describe('FormValidationHelper', () => {
  it('should create an instance', () => {
    expect(new FormValidationHelper()).toBeTruthy();
  });
});
